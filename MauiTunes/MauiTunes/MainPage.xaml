<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiTunes.MainPage">

    <ScrollView>

        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Start">

            <Label Text="Search albums"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="4*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Entry x:Name="entryAlbums"
                       Grid.Column="0"/>
                <Button Clicked="Button_Clicked" Text="Search"
                        Grid.Column="1" BackgroundColor="DarkGrey"/>
            </Grid>


            <CollectionView x:Name="albumsList">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <VerticalStackLayout>
                            <Button Text="{Binding Name}" Clicked="Button_Clicked_1Async"/>
                            <Label Text="{Binding Artists[0].Name}" BackgroundColor="ForestGreen"/>
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>

    </ScrollView>

</ContentPage>
